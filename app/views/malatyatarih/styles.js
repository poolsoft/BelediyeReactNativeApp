import { Dimensions } from "react-native";

const { width } = Dimensions.get('window')

export default {
  a: {
    fontWeight: '400',
    color: '#FF8E00', // make links coloured pink
  },
  title:
  {
    left:0,
    width:200
  }
};